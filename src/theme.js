import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#0C1E21'
    },
    text: {
      textColor: '#fff' // Define your primary color here
    }
    // Add other colors as needed
  }
});
